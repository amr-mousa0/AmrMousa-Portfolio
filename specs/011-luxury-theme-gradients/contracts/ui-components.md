# UI Contracts: Luxury Theme Gradients

No external API contracts or CLI arguments are modified. The contracts are visual and bound to CSS variables.

## CSS Custom Properties

Expected definitions in `index.html`:

- `--bg-primary`: Used as the base background color. Will be layered with a gradient.
- `--bg-secondary`: May be used in the gradient composition.
