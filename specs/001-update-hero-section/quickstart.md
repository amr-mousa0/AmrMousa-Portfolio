# Quickstart: Update Hero Section

Since this is a static HTML update, no backend compilation, installation, or heavy setup is required.

## How to View the Changes

1. Open `index.html` in your preferred modern web browser (e.g., Chrome, Edge, Safari).
2. Verify that the Hero Section renders correctly and the styling matches expectations.
3. Test all the links in the Hero Section to ensure they point to the correct destinations (Social links, "Download CV", and "Contact").
