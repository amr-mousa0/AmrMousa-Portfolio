---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  description?: string;
  keywords?: string;
  image?: string;
  lang?: string;
  dir?: string;
  githubUrl?: string;
  isProjectPage?: boolean;
}

const props = Astro.props;
---
<BaseLayout {...props}>
  <slot />
</BaseLayout>
